{
  "name": "backbeat",
  "version": "0.1.0",
  "description": "Backbeat ERP System - Stash (Inventory Management) Module",
  "private": true,
  "scripts": {
    "dev": "npm run dev:server & npm run dev:client",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "seed": "cd server && npm run seed",
    "start": "cd server && npm start",
    "test:e2e": "npx playwright test; cd server && npm run seed"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "pg": "^8.18.0"
  }
}
